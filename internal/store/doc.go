// Package store owns the embedded SQLite datastore (magichandy.db): connection
// setup, schema migrations, one-time JSON import, and durable rows for settings,
// memories, and prompt sets.
package store
